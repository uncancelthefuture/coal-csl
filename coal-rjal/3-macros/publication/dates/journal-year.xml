<macro name="journal-year">
 <choose>
  <if variable="issued">
   <date date-parts="year" form="text" variable="issued" prefix="(" suffix=")"/>
  </if>
  <else>
   <text term="no date"/>
  </else>
 </choose>
</macro>
